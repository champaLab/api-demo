name: Test Runner

on:
  push:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a test command
        run: echo "Runner is working!"
